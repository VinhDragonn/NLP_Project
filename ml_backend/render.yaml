services:
  - type: web
    name: nlp-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn nlp_service:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: NLP_PORT
        value: 8002
